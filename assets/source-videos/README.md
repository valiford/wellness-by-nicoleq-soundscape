# Supplied visual source videos

These files are the original supplied Gemini MP4s. They are retained outside `public/` so the original audio tracks and source bitrate are preserved without adding them to the production payload.

- `pearl-flow-source.mp4`: supplied `Create_a_seamless_looping_luxu.mp4`
- `violet-flow-source.mp4`: supplied `Generate_a_seamless_looping_at (1).mp4`
- `chakra-flow-source.mp4`: supplied `Create_a_seamless_looping_ambi (1).mp4`

The runtime uses the silent derivatives under `public/media/environments/optimized/`.
